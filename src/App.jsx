import React from 'react';
import { AppContext } from './context/contextApi';

const App = () => {
  return (
    <AppContext>
      <h2 className='text-red-600'>shuvo</h2>
    </AppContext>
  );
};

export default App;
